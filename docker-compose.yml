name: concurso

services:
  nginx:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ${PHOTOS_OUT_DIR}:/var/lib/concurso/fotos:ro
    depends_on:
      - frontend
      - api

  mongo:
    image: mongo:6
    restart: unless-stopped
    volumes:
      - mongo-data:/data/db

  api:
    build:
      context: .
      dockerfile: server/Dockerfile
    environment:
      - PORT=3000
      - MONGODB_URI=mongodb://mongo:27017/concurso
      - PHOTOS_COLLECTION=photos
    ports:
      - "3000:3000"
    volumes:
      - ./server:/usr/src/app/server
    depends_on:
      - mongo

  frontend:
    build:
      context: ./static
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    environment:
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - ./static:/app
    depends_on:
      - api

  ingest:
    image: python:3.12-slim
    restart: "no"
    working_dir: /app
    command: sh -c "pip install -r /app/requirements.txt && python -m photo_ingest"
    environment:
      - MONGODB_URI=mongodb://mongo:27017/concurso
      - PHOTOS_COLLECTION=photos
      - PHOTOS_DIR=/photos
      - PHOTOS_OUT_DIR=/photos_out
    volumes:
      - ./tools/photo_ingest:/app
      - ${PHOTOS_DIR}:/photos:ro
      - ${PHOTOS_OUT_DIR}:/photos_out:rw
    depends_on:
      - mongo

volumes:
  mongo-data:
